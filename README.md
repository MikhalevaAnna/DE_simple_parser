# Разработка простого парсера

## Задание:
Если парсить нужно что-то маленькое и простое, то в большинстве случаев с задачей прекрасно справится дуэт из `Requests` и `BeautifulSoup`. </br>
1. Выберите любой из предложенных ниже сайтов для парсинга (выбрала `books.toscrape.com`).
2. Напишите кастомную конфигурацию, в которую будут ходить настройки заголовка, задержи между запросами, количество повторных попыток при статус-коде != 200.
### Требования к выполнению задания:
- Парсер собирает минимум три поля информации;
- Парсер обрабатывает ошибки соедниения;
- Парсер работает с пагинацией: обходит все страницы каталога
- Парсер сохраняет все данные в csv, с помощью `Pandas/PySpark` проводится агрегация, проверка на целостность данных
- Фильтрованные данные загружаются в БД: `Postgres/Хранилище Selectel`
### Требования к коду:
- Используйте функции для модульности и читаемые названия переменных
- Комментируйте сложные участки кода
- Проверьте Ваш код на соответствие `PEP8` (можно через `codestyle`)</br>

Задание будет считаться выполненным, если соблюдены все требования к коду, а также выполнено от 3 до 5 заданий, количество баллов за задание выдается соответственно.</br>

## Реализация:
Для парсинга выбран сайт `books.toscrape.com`. </br>

1. Парсер собирает информацию по следующим полям:
   - `title` - Название книги,
   - `price` - Стоимость,
   - `rating` - Рейтинг,
   - `stock` - Количество книг в наличии,
   - `in_stock` - Статус товара (в наличии/отсутствует),
   - `url` - Ссылка на карточку товара,
   - `upc` - Уникальный идентификатор (Universal Product Code),
   - `product_type` - Тип товара (Books),
   - `price_excl_tax` - Стоимость без налога,
   - `price_incl_tax` - Стоимость с налогом,
   - `tax` - Налог (не заполнен),
   - `category` - Категория,
   - `image_url` - URL изображения. </br>

2. Парсер обрабатывает ошибки соединения. </br>

3. Парсер сохраняет все данные в **CSV**, проводится агрегация и проверка целостности. </br>

4. В результате работы парсера формируются файлы, которые сохраняются локально в папке `files`, а также в `S3 Selectel` хранилище.  </br>
5. В `S3 Selectel хранилище` файлы сохраняются в бакете `de-books`, можно посмотреть в папке `screenshots`.</br>
6. Структура проекта:
 ```
 📂 D:\DE_simple_parser/
 ├── 📁 files/    # CSV файлы с полученными данными то же самое лежит в S3, только названия файлов меняются
 │   └── 📄 all_books_detailed.csv    # Все данные, которые удалось распарсить с сайта books.toscrape.com
 │   └── 📄 book_statistics.csv       # Файл со статистикой по скаченным данным
 │   └── 📄 data_quality_report.csv   # Аналитический отчет по данным 
 │   └── 📄 filtered_books.csv        # Файл с отфильтрованными данными, где рейтинг >= 4, цена <= £50.0
 ├── 📁 logs/     # Лог файлы
 │   └── 📄 books_parser.log   # Файл с логами
 ├── 📁 screenshots/           # Скриншоты наличия файлов в бакете de-books
 │   └── 📄 all_in_bucket.png         # Созданные папки в бакете. Описание ниже. 
 │   └── 📄 analysis_results.png      # Содержимое папки analysis_result
 │   └── 📄 filtered_data.png         # Содержимое папки filtered_data
 │   └── 📄 raw_data.png              # Содержимое папки raw_data 
 │   └── 📄 statistics.png            # Содержимое папки statistics 
 ├── 📁 src/                   # Исходный код
 │   └── 📄 s3_interaction.py         # Функции для работы с S3 Selectel
 │   └── 📄 data_analyze.py           # Функции для анализа данных и генерации отчетов
 │   └── 📄 constants.py              # Константы и директории
 │   └── 📄 logger.py                 # Настройка логирования для всего приложения
 └── 📄 .env.example                  # Пример файла для настройки доступов к S3
 └── 📄 .gitignore                                         
 └── 📄 books_parser.py   # Основной скрипт. Парсер сайта books.toscrape.com с сохранением в **S3 Selectel** бакет.
 └── 📄 requirements.txt  # Файл с зависимостями
 ```
 Подробнее о папках и файлах в бакете  `de-books`:
  - Файлы в папках бакета `de-books` это те же файлы, которые лежат в папке `files`, только в качестве префикса в названиях файлов в бакете используется `timestamp`;
     
  - **raw_data/** - содержит файлы вида `20260201_204939_all_books_detailed.csv` - со всеми данными с сайта, всего 1000 книг;

  - **cleaned_data/** - сохраняем сюда файлы без дублирования `20260201_204939_books_cleaned.csv`, в нашем случае дублей в данных нет, поэтому файлы не созданы;
  
  - **filtered_data/** - сохраняются файлы после фильтрации `20260201_204940_filtered_books.csv`;</br>
    - Условия фильтрации данных: рейтинг >= 4, цена <= £50.0, всего этому условию соотвествует 290 книг из 1000. Используется `Pandas`.</br>
    
  - **statistics/** - сохраняются данные в виде статистики по исходным данным в файлах вида `20260201_204940_book_statistics.csv`.</br>
 ```
    - Для очистки и аналитики данных и  используется `Pandas`.
    - Считается статистика по рейтингам, средние, минимальные и максимальные цены,
        количество карточек товара, количество книг.
    - Обнаруживаются выбросы.
    - Проверяется качество данных.
    - Удаляются полные дубли.
 ```
  - **analysis_result/** - сохраняются данные в виде общего аналитического отчета по исходным данным в файлах вида `20260201_204940_data_quality_report.csv`.</br>

7. В коде присутствуют комментарии. </br>
8. Код проверен на соответствие **PEP8**. </br>
9. Запуск проекта:
 ```
 1) git clone git@github.com:MikhalevaAnna/DE_simple_parser.git
 2) cd DE_simple_parser
 3) pip install -r requirements.txt
 4) cp .env.example .env           # Отредактируйте .env файл, добавьте свои ключи
 5) python books_parser.py
 ```

